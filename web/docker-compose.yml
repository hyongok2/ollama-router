services:
  ollama-router-web:
    image: ollama-router-web:v0.0.2
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ollama-router-web
    ports:
      - "5800:80"
    restart: unless-stopped
